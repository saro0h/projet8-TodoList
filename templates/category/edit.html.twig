{% extends 'base.html.twig' %}

{% block header_title %}<h1>{{ 'app.edit'|trans }} <strong>{{ category.title }}</strong></h1>{% endblock %}
{% block header_img %}{% endblock %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'action': path('category_edit', {'id': category.id})}) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-success pull-right">{{ 'app.edit'|trans }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
